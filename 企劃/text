幫我將以下企劃添加到這個網頁中

網頁分類
    1. 登陸頁
    2. 伺服器列表頁
        登陸後系統會列出該使用者所有的Discord 系統會判定每一個伺服器是否有 [管理員機器人]
        有的會往前排 沒有的會在後面(使用按鈕進行分類)
    3. 伺服器首頁
        1. 顯示該使用這在伺服器的管理權限(一般使用者、管理員(/admin指令)、伺服器擁有者)
        2. 顯示該用戶在這個伺服器被登記的警告和嘉獎數量
        3. 顯示該使用者在這個伺服器的警告和嘉獎紀錄(Log)，並且可以使用搜尋功能來快速找到特定紀錄
        4. 顯示這個伺服器的懲處和獎勵設定(自動化功能)
    4. 模板頁
        1. 顯示所有模板列表，並且可以使用搜尋功能和分類按鈕來快速找到模板
        2. 上傳模板功能 : 任何使用者都可以上傳模板，但需要經過管理員審核才能公開顯示在模板列表中
        3. 模板審核列表 : 任何使用者都可以查看自己上傳的模板審核狀態
        4. 模板審核功能(只有開發者和管理員帳戶可以看到) : 可以進行模板的審核
        5. 模板紀錄功能(只有開發者可以看到) : 可以查看所有模板的操作紀錄，包括上傳、審核、刪除等操作的使用者資訊和操作時間等紀錄，並且有按鈕分類顯示是上傳紀錄、審核紀錄還是刪除紀錄
    5. 成員管理頁(只有伺服器擁有者和擁有管理員權限的成員可以使用這些功能)
        1. 查看所有成員的警告和嘉獎紀錄，並且可以使用搜尋功能來快速找到特定成員的紀錄
        2. 給予成員警告或嘉獎，並且可以登陸新的警告或嘉獎紀錄，並將紀錄登陸到資料庫中，包含成員名稱、類型（警告或嘉獎）、原因和日期
        3. 設定當成員的警告或嘉獎達到一定數量時，會自動執行懲處或獎勵的動作，並且可以設定嘉獎是否可以和警告抵消，當成員有嘉獎紀錄時可以抵消掉相同數量的警告紀錄
    6. 開發者限定頁
        1. 可以使用/message同樣功能的系統
        2. 可以使用/server_info同樣功能的系統

開發者帳戶 :
    可以顯示所有頁面
管理員帳戶 :
    可以顯示除了開發者限定頁以外的所有頁面
一般使用者帳戶 :
    只能顯示伺服器首頁和模板頁，並且在伺服器首頁只能看到自己在這個伺服器被記錄的警告和嘉獎紀錄
    (伺服器擁有者和擁有管理員權限的成員限定)可以使用成員管理頁的功能